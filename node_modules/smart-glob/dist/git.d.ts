import { GlobOptions } from './glob';
declare type GitGlobOptions = Pick<GlobOptions, 'cwd' | 'absolute' | 'ignoreGlobs' | 'gitignore' | 'alwaysReturnUnixPaths'> & {
    gitFlags?: string;
};
export declare function globWithGit(globStr: string, opts?: GitGlobOptions): Promise<string[]>;
export declare function globWithGitSync(globStr: string, opts?: GitGlobOptions): string[];
export declare function gitPaths({ cwd, gitFlags, gitignore, }: {
    cwd?: string;
    gitFlags: any;
    gitignore?: boolean;
}): Promise<string[]>;
export declare function gitPathsSync({ cwd, gitFlags, gitignore, }: {
    cwd?: string;
    gitFlags: any;
    gitignore?: boolean;
}): string[];
export {};
